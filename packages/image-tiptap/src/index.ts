import ImageAligner from "./components/image-aligner";
import ImageExtension from "./extensions/image-extension";

/**
ImageExtension is a class that extends the Editor class with image-related functionalities.
ImageAligner is a component that aligns images to the left, center, or right, as well as resizing them.
*/
export { ImageAligner, ImageExtension };
